workers = 2  # The number of worker processes
bind = "127.0.0.1:8000"  # The host and port where your app should be served
module = "app:app"  # Replace "app" with the name of your Flask application object
